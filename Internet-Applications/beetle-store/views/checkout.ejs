<!DOCTYPE html>
<html>
    <head>
        <title>Checkout - Beetle Store</title>
        <link rel="stylesheet" href="/css/checkout-style.css">
    </head>
    <body>
        <h1>Shopping Cart</h1>
        <div>
            <% if (cart.length > 0) { %>
            <ul>
                <% cart.forEach(item => { %>
                <li>
                    <h2><%= item.name %></h2>
                    <p>Quantity: <%= item.quantity %></p>
                    <p>Price: $<%= item.price %></p>
                    <!-- Add logic to update quantity or remove items if needed -->
                </li>
                <% }); %>
            </ul>
            <form action="/checkout/finalize" method="POST">
                <button id="finalize" type="submit">Finalize Purchase</button>
            </form>
            <form action="/checkout/cancel" method="POST">
                <button id="cancel" type="submit">Cancel Purchase</button>
            </form>
            <% } else { %>
            <p>Your cart is empty.</p>
            <% } %>
        </div>
        <a href="/">Continue Shopping</a>
    </body>
</html>
